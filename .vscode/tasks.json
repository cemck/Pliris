{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "save_file",
      "command": "${workbench.action.files.save}"
    },
    {
      "label": "make_command",
      "type": "shell",
      "command": "cd ${workspaceFolder} && make package install"
    },
    {
      "label": "save_and_make",
      "dependsOn": ["save_file", "make_command"]
    }
  ]
}
